name: 🧪 Lint and Test

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  test:
    name: 🧪 test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [0.10.x, 0.12, 4, 8, 12, 16, 20]
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - name: 📦 Install Dependencies
      - run: npm ci
      - name: 🧪 lint and test
      - run: npm test
