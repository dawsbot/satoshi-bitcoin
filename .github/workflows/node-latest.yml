name: node latest

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

jobs:
  node-latest:
    uses: ./.github/workflows/test-reusable.yml
    with:
      node-version: "latest"
